# apple.nu

Simple static contact website for `apple.nu`.

## Files

- `index.html` contains the page markup.
- `style.css` contains all styling.
- `netlify.toml` configures Netlify to publish the repository root.

## Deploy

Connect this repository to Netlify and use the default static site settings. No build command is required.
